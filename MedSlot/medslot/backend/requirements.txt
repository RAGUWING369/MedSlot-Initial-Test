# MedSlot Backend — Python Dependencies
# All packages are pinned to exact versions for reproducible builds.
# Run: pip install -r requirements.txt

# ── Core Django ────────────────────────────────────────────────────────────────
Django==5.0.9
djangorestframework==3.15.2
djangorestframework-simplejwt==5.3.1
django-redis==5.4.0
django-cors-headers==4.4.0

# ── Celery ─────────────────────────────────────────────────────────────────────
celery[redis]==5.4.0
django-celery-beat==2.7.0

# ── Database ───────────────────────────────────────────────────────────────────
psycopg2-binary==2.9.9

# ── AWS / Storage ──────────────────────────────────────────────────────────────
boto3==1.35.0

# ── Email ──────────────────────────────────────────────────────────────────────
sendgrid==6.11.0

# ── Payments ───────────────────────────────────────────────────────────────────
razorpay==1.4.2

# ── PDF Generation ─────────────────────────────────────────────────────────────
WeasyPrint==60.2

# ── API Documentation ──────────────────────────────────────────────────────────
drf-spectacular==0.27.2

# ── Configuration & Logging ────────────────────────────────────────────────────
python-decouple==3.8
python-json-logger==2.0.7

# ── WSGI Server ────────────────────────────────────────────────────────────────
gunicorn==22.0.0

# ── Development & Testing ──────────────────────────────────────────────────────
pytest==8.3.2
pytest-django==4.8.0
pytest-cov==5.0.0
factory-boy==3.3.1
Faker==26.0.0

# ── Code Quality ───────────────────────────────────────────────────────────────
black==24.8.0
isort==5.13.2
flake8==7.1.1
